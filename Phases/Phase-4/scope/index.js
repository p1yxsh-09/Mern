function collectEggs() {
  let totalEggs = 6;
  console.log(totalEggs);
} // this is a fucntion scope where the function is only accesible to that and out of that u cant access it , u can make the variable golabal then u can do

collectEggs();
console.log(totalEggs);
